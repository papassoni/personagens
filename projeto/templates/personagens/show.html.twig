{% extends 'base.html.twig' %}

{% block title %}Personagens{% endblock %}

{% block body %}
    <h1>Personagens</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ personagen.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ personagen.nome }}</td>
            </tr>
            <tr>
                <th>Detalhes</th>
                <td>{{ personagen.detalhes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('personagens_index') }}">back to list</a>

    <a href="{{ path('personagens_edit', {'id': personagen.id}) }}">edit</a>

    {{ include('personagens/_delete_form.html.twig') }}
{% endblock %}
